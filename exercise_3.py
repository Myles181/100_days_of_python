#!/usr/bin/python3
"""
   Calculate the area of a room
   Area = length * breath
"""
length = float(input('Input length(m): '))
breath = float(input('Input breath(m): '))
Area = length * breath
print('Area = {} m'.format(Area))
